const { gets, print } = require("./funcoes_auxiliares2");

const NUMEROS = gets();

let resultado = "";
let maior = 0;
let menor = 0;

for (let i = 0; i < NUMEROS; i++) {
  if (NUMEROS > menor) {
//    menor += NUMEROS
    console.log(NUMEROS)
  }
//  if (NUMEROS > menor) {
//    console.log(`${NUMEROS}`)
//  }
//  const MAIOR = Math.max(SELECAO);
//  const MENOR = Math.min(SELECAO);
//  if ()
//    resultado = `O MAIOR valor é ${MAIOR} e o MENOR ${MENOR}`;
//    resultado = `O menor valor é ${MENOR}`;

  //  if (SELECAO > maiorValorEncontrado) {
  //    maiorValorEncontrado = numeroSorteado;
  //  }
}

print(resultado);
